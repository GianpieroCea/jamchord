// ChordProgression.js

import React from 'react';

const ChordProgression = ({ name }) => {
    return (
        <div>
            <h2>{name}</h2> {/* Display the name of the chord progression */}
            {/* Display the chord progression */}
        </div>
    );
};

export default ChordProgression;
